from tkinter import *

# Create a root element
root = Tk()

# Define the size of the window in pixels
root.geometry('700x500')

# Launch the window
root.title('Window')
root.resizable(height = False, width = False) # Non-resizable window
root.mainloop()

